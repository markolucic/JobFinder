<%= form_for :job, url: jobs_path, :html => {:class => "form-horizontal center"} do |f| %>
	<% if @job.errors.any? %>
	 <div id="error_explanation">
	 	<h2>
	 		<%= pluralize(@job.erorrs.count, "error") %> prohibited this article from being saved:
	 	</h2>
	 	<ul>
	 		<% @job.erorrs.full_messages.each do |m| %>
	 			<li> <%= m %></li>
	 		<% end %>
	 	</ul>
	 </div>
	<% end %>

	<div class="form-group">
	 <%= f.label :title %></br>
	 <%= f.text_field :title %>
	</div>

	<div class="form-group">
		<%= f.label :description %></br>
		<%= f.text_area :description %>
	</div>

	<div class="form-group">
	 <%= f.label "Email address" %></br>
	 <%= f.text_field :email %>
	</div>

	<div class="form-group">
	 <%= f.label :city %></br>
	 <%= f.text_field :city %>
	</div>

	<div class="form-group">
	 <%= f.label :salary %></br>
	 <%= f.text_field :salary %>
	</div>

	<div class="form-group">
	 <%= f.label :experience %></br>
	 <%= f.check_box :experience, {}, true, false %> 
	</div>

	<div > <%= f.submit "Submit", class: "btn btn-default" %> </p>
<% end %>